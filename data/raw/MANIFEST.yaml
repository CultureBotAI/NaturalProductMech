# Provenance for every committed inventory in data/raw/.
#
# `just provenance-check` requires that every inventory here matches its
# recorded sha256, and that every inventory file on disk is recorded.
# Empty until M2 extracts the first one.
upstream: {}
inventories: {}
